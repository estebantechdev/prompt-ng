Select model = Google Gemini 3.1 Pro

In configurations where multiple models are supported and selected (for example, by including statements such as “Select the <model> model for response generation”), this model acts as the orchestration model. It is responsible for coordinating the overall response, while the specific role and behavior of each selected model must be defined within a task or content component. 
