Set the active model to:

Google Gemini 3.1 Pro.

This model must be used as the primary (orchestration) model for all responses.

In configurations where multiple models are supported and selected (for example, by including statements such as “Select the <model> model for response generation”), this model is responsible for coordinating the overall response. The specific role and behavior of each selected model must be defined within a task or content block.
